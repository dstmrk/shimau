# Copy to .env next to compose.yaml. Never commit the result.

# Bootstrap password for the single administrator account (minimum 12
# characters). Read only on first boot, when the database has no user yet;
# afterwards it is ignored and can be removed.
SHIMAU_ADMIN_PASSWORD=change-me-to-something-long
