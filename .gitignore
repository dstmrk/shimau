# Rust
/backend/target

# Node
/frontend/node_modules
/frontend/dist
/frontend/coverage

# Local runtime data of a self-hosted instance
/data

# Environment files — the example is the only one that is committed
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
*.swp
